================
글쓰기(파일 업로드 전)

DB - Sequence 생성



------
글쓰기
------
1 list.jsp 글쓰기 버튼 클릭 -> /Board/post.do (flag 미포함) -> post form 화면
2 post form 회면에 내용 입력 , 글쓰기 버튼 (flag포함) ->
BoardPostController
1) 글관련 파라미터 받기
2) 입력값검증
3) 서비스 실행 -> Dao -> DB
4) View 로 이동

